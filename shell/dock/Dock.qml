// SPDX-License-Identifier: GPL-3.0-or-later
import QtQuick
import Dragonfruit

// Dock placeholder — T-10 builds the real Dock on this module.
Rectangle {
    id: dock
    height: 64
    color: Theme.surface
    radius: Theme.radius
}
