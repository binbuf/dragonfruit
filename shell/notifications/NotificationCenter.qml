// SPDX-License-Identifier: MIT
import QtQuick
import Dragonfruit

// Notification center placeholder — T-25 builds the real queue and UI.
Rectangle {
    id: notificationCenter
    color: Theme.color.surface
    radius: Theme.controls.window.radius
}
