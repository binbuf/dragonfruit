// SPDX-License-Identifier: MIT
// Settings menu-model publication tests (T-09.6a): the app's published model
// is asserted in shape and round-trips into the shell's `MenuBar`. Headless on
// the offscreen platform with the software scene graph.
#include <QtQuickTest>

QUICK_TEST_MAIN(tst_settings_menu)