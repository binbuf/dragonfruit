# T-14.1a — app-index identity resolution and icons

| | |
|---|---|
| **Order** | 100 of 168 |
| **Slice** | T-14 |
| **Area** | services/app-index |
| **Depends on** | T-09.6b |
| **Blocks** | T-14.1b |
| **Hardware** | no |

> Parent design reference: [../14-global-menu-app-index-compat.md](../design/tracks/14-global-menu-app-index-compat.md)

## Goal

`org.dragonfruit.AppIndex1` identity resolution and themed icons; retire the Dock stub resolver.

## Scope

- `org.dragonfruit.AppIndex1`: identity resolution (`app_id`/`WM_CLASS`/heuristics), themed icons.
- Retire the Dock's stub resolver; the miss set is the heuristic input.

## Explicitly deferred

- Events, launch registry, recency (T-14.1b), subscription (T-14.1c).

## Acceptance (agent-completable)

- [ ] Identity resolves for Wayland and X11 fixtures; the stub resolver is gone.

## Test plan

- Headless: fixture window resolves to the expected desktop entry/icon.

## Hand-off

- T-14.1b adds events.

> Human sign-off (watch the demo, compare to design docs/goldens) is batched at
> the track boundary, not part of this unit. See
> [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
