# T-15.1b — Bluetooth pane and tile

| | |
|---|---|
| **Order** | 112 of 168 |
| **Slice** | T-15 |
| **Area** | apps/settings, shell/ (Control Center) |
| **Depends on** | T-15.1a |
| **Blocks** | T-15.2a |
| **Hardware** | no |

> Parent design reference: [../15-system-services-breadth.md](../design/tracks/15-system-services-breadth.md)

## Goal

Settings pane and Control Center tile for Bluetooth as one functional unit.

## Scope

- Settings pane and Control Center tile shipped together (no half-panes).
- Absence case documented.

## Explicitly deferred

- None.

## Reference UI/UX (macOS screenshots)

Near-verbatim target: the real macOS screenshots in `docs/reference/macos/`
(never shipped; see [14-risks.md](../design/14-risks.md)), distilled in
[System_Preferences.md](../reference/System_Preferences.md).

**Sections and rows (observed order):** Bluetooth toggle card — icon,
`Bluetooth` toggle, and the caption `This <device> is discoverable as
"<device>"` while the pane is open; `My Devices` — device rows with name,
connection state (`Not Connected`), and a circular info button; `Nearby
Devices` — `Searching...` with a spinner. The pane maps to the BlueZ adapter
(T-15.1a) and ships with the Control Center tile as one unit; the
quick-settings sheet shows the same toggle plus the connected-devices list,
with `Bluetooth Settings...` opening the pane.

**Adapt or omit (Apple-only):** the `Learn more...` web link, the `?` help
button, and the Apple Account sidebar row. The captured device names are
generic Bluetooth names (`WF-1000XM6`, `WH-1000XM6`) and can stay as
fixture-like examples.

## Acceptance (agent-completable)

- [ ] Pane applies live; tile reflects state; absence documented.

## Test plan

- Headless: pane controls round-trip through settingsd/the adapter.

## Hand-off

- Next subsystem or the breadth capture.

## Live visual check (required)

Before reporting done, run the app and **look at it**:

- Launch the nested session (`make demo`), open the surface this task changed,
  and capture it (e.g. `spectacle -b -n -f -o /tmp/<task>.png`).
- Inspect the image: all UI/UX must be present and read as intended in the
  compositor window — layout, materials, text, and no stray artifacts.
- A green `make e2e` is necessary but **not sufficient**: the headless suites
  render no pixels, so they miss exactly this class of bug. If the task has no
  surface of its own, still launch the demo and confirm the desktop renders.
- Note the capture path and your verdict in the Hand-off.

> The per-task live visual check above is part of this unit. Only the *human*
> sign-off (watch the demo, compare to design docs/goldens) is batched at the
> track boundary. See [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
