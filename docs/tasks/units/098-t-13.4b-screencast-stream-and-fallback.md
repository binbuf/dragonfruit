# T-13.4b — ScreenCast stream and stills fallback

| | |
|---|---|
| **Order** | 98 of 168 |
| **Slice** | T-13 |
| **Area** | portal/ |
| **Depends on** | T-13.4a |
| **Blocks** | T-13.5a |
| **Hardware** | no |

> Parent design reference: [../13-portals-capture-clipboard.md](../13-portals-capture-clipboard.md)

## Goal

Stream the chosen source through PipeWire; document a stills-only fallback if timeboxed.

## Scope

- The stream is visible in the client.
- Timebox; a stills-only fallback is documented if needed.

## Explicitly deferred

- None.

## Acceptance (agent-completable)

- [ ] A client receives frames; if timeboxed out, the fallback is named, not silent.

## Test plan

- Headless: stream negotiation produces a PipeWire node (or the documented fallback).

## Hand-off

- T-13.5 adds clipboard.

> Human sign-off (watch the demo, compare to design docs/goldens) is batched at
> the track boundary, not part of this unit. See
> [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
