# T-05.4 — Image wallpaper and per-Space slide

| | |
|---|---|
| **Order** | 22 of 104 |
| **Slice** | T-05 — Loop v3 — Mission Control live |
| **Area** | compositor/ + shell/overview |
| **Depends on** | T-05.3 |
| **Blocks** | T-05.5 |
| **Hardware** | no |

> Parent design reference: [../05-loop-v3-mission-control-live.md](../05-loop-v3-mission-control-live.md)

## Goal

Sample `source`/`fit`, render per Space, and slide the wallpaper with the Space during a switch.

## Scope

- Image wallpaper with fit; no image configured → current color behavior.
- Large images cached and scaled without per-frame decode.

## Explicitly deferred

- Per-output wallpaper animation polish (T-16).

## Acceptance (agent-completable)

- [ ] The wallpaper image moves with its Space; the color fallback still works.

## Test plan

- Headless wallpaper sampling unit tests; nested capture.

## Hand-off

- T-05.5 reuses the pipeline for Desktop Reveal.

> Human sign-off (watch the demo, compare to design docs/goldens) is batched at
> the track boundary, not part of this unit. See
> [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
