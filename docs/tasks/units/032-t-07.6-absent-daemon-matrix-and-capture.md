# T-07.6 — Absent-daemon matrix and capture

| | |
|---|---|
| **Order** | 32 of 104 |
| **Slice** | T-07 — Menu bar goes live |
| **Area** | services/ + shell/menubar |
| **Depends on** | T-07.5 |
| **Blocks** | T-11.1 |
| **Hardware** | no |

> Parent design reference: [../07-menu-bar-live-status.md](../07-menu-bar-live-status.md)

## Goal

Prove graceful degradation with all three daemons absent and keep idle wakeups at zero.

## Scope

- Mask/stop each daemon; the item hides or says 'unavailable'; the session is unaffected.
- Idle trace: the menu bar contributes zero wakeups.
- Commit `docs/captures/t07-*` including an absent-daemon still.

## Explicitly deferred

- Bluetooth/other adapters (T-15).

## Acceptance (agent-completable)

- [ ] The absent-daemon matrix passes; idle trace flat; capture committed.

## Test plan

- Mock in CI; real masking in a VM when available; idle trace.

## Hand-off

- T-11 consumes the adapters for Control Center.

> Human sign-off (watch the demo, compare to design docs/goldens) is batched at
> the track boundary, not part of this unit. See
> [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
