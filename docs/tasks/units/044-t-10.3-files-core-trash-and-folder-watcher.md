# T-10.3 — files-core trash and folder watcher

| | |
|---|---|
| **Order** | 44 of 104 |
| **Slice** | T-10 — Files MVP |
| **Area** | services/files-core |
| **Depends on** | T-10.2 |
| **Blocks** | T-10.4 |
| **Hardware** | no |

> Parent design reference: [../10-files-mvp.md](../10-files-mvp.md)

## Goal

Trash via the freedesktop spec/GVfs plus a folder watcher as the single source of truth.

## Scope

- Trash (freedesktop spec via GIO/GVfs when available; sanctioned fallback otherwise).
- Folder watcher; GVfs fallback marked for replacement, not silently shipped.

## Explicitly deferred

- Drop-to-trash UI (T-10.6).

## Acceptance (agent-completable)

- [ ] Trash spec round-trips; watcher events fire on third-party changes.

## Test plan

- Unit: trash spec round-trips; watcher events.

## Hand-off

- T-10.4 renders it.

> Human sign-off (watch the demo, compare to design docs/goldens) is batched at
> the track boundary, not part of this unit. See
> [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
