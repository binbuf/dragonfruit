# T-10.1 — files-core streaming listing and model

| | |
|---|---|
| **Order** | 42 of 104 |
| **Slice** | T-10 — Files MVP |
| **Area** | services/files-core |
| **Depends on** | T-08.3 |
| **Blocks** | T-10.2 |
| **Hardware** | no |

> Parent design reference: [../10-files-mvp.md](../10-files-mvp.md)

## Goal

Build files-core's streaming directory listing, sorting, and model.

## Scope

- Streaming directory listing, sorting, and the file/folder model.
- Reuse platform plumbing (GIO/GVfs when available; sanctioned fallback otherwise, marked for replacement).

## Explicitly deferred

- FileChooser portal consumers (T-13).
- Indexer/search (post-gate).

## Acceptance (agent-completable)

- [ ] Listing streams without blocking; unit tests pass against a temp tree.

## Test plan

- Unit: model and listing against a temp tree.

## Hand-off

- T-10.2 adds operations.

> Human sign-off (watch the demo, compare to design docs/goldens) is batched at
> the track boundary, not part of this unit. See
> [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
