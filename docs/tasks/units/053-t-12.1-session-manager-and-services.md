# T-12.1 — Session manager and services

| | |
|---|---|
| **Order** | 53 of 104 |
| **Slice** | T-12 — Session + lock + idle |
| **Area** | services/session/ + systemd units |
| **Depends on** | T-08.3 |
| **Blocks** | T-12.2 |
| **Hardware** | no |

> Parent design reference: [../12-session-lock-idle.md](../12-session-lock-idle.md)

## Goal

Launch order, restart policy, and environment for the Dragonfruit session (nested-testable).

## Scope

- Session manager with launch order and restart policy.
- Environment: `XDG_CURRENT_DESKTOP`, `WAYLAND_DISPLAY`, `DISPLAY`, `DRAGONFRUIT_LAUNCH_TOKEN`.
- systemd user units; dedicated-user second-VT workflow for development.

## Explicitly deferred

- DRM-only session entry validation (T-03 rail).
- Fast user switching.

## Acceptance (agent-completable)

- [ ] Shell/services restart; compositor death ends the session by design; tokens are per shell start.

## Test plan

- Headless/nested: shell/service restart, token hand-off; unit: session env, unit files.

## Hand-off

- T-12.2 adds the DM entry.

> Human sign-off (watch the demo, compare to design docs/goldens) is batched at
> the track boundary, not part of this unit. See
> [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
