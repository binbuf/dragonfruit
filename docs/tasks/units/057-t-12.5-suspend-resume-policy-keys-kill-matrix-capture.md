# T-12.5 — Suspend/resume, policy keys, kill matrix, capture

| | |
|---|---|
| **Order** | 57 of 104 |
| **Slice** | T-12 — Session + lock + idle |
| **Area** | services/session/ |
| **Depends on** | T-12.4 |
| **Blocks** | T-13.1 |
| **Hardware** | no |

> Parent design reference: [../12-session-lock-idle.md](../12-session-lock-idle.md)

## Goal

Complete session lifecycle: one suspend/resume cycle, policy keys in settingsd, and the kill matrix.

## Scope

- One suspend/resume cycle with clean recovery of outputs, input, and clients.
- Policy keys in settingsd: lock delay, idle delays, suspend behavior.
- Kill matrix: lock UI, shell, settingsd, notification service.
- Commit `docs/captures/t12-session.*`.

## Explicitly deferred

- 100-cycle soak, multi-monitor transitions, fractional scaling (T-16).

## Acceptance (agent-completable)

- [ ] One suspend/resume recovers cleanly; policy keys round-trip; kill matrix passes; capture committed (human sign-off batched).

## Test plan

- DRM: login → lock → idle → suspend → logout, repeated.
- Kill tests.

## Hand-off

- T-13 validates portals in this session.

> Human sign-off (watch the demo, compare to design docs/goldens) is batched at
> the track boundary, not part of this unit. See
> [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
