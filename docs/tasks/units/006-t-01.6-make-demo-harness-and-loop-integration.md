# T-01.6 — make demo harness and loop integration

| | |
|---|---|
| **Order** | 6 of 104 |
| **Slice** | T-01 — Loop v0 — window controls |
| **Area** | scripts/ + shell/ wiring |
| **Depends on** | T-01.5 |
| **Blocks** | T-02.1 · T-07.1 · T-08.1 |
| **Hardware** | no |

> Parent design reference: [../01-loop-v0-window-controls.md](../01-loop-v0-window-controls.md)

## Goal

One command that builds, launches the nested session with the shell and two apps, and prints the demo checklist; prove the existing Dock/menu-bar paths through the new UI.

## Scope

- `make demo`: build + nested launch + shell + one Qt app + one X11 app + printed checklist.
- Same path CI uses for the scripted half.
- Dock/menu-bar integration check: focused app name, running indicator, minimized entry, restore, close resolves the Dock entry.
- Commit `docs/captures/t01-*` stills/recording.

## Explicitly deferred

- Motion in the demo (T-02).

## Acceptance (agent-completable)

- [ ] `make demo` runs the loop in a live nested session.
- [ ] Capture is committed; integration steps pass; `make e2e` and `make soak` stay green.

## Test plan

- Nested: the printed checklist.
- CI: the same launch path with the scripted assertions.

## Hand-off

- Every later slice's demo builds on this command.

> Human sign-off (watch the demo, compare to design docs/goldens) is batched at
> the track boundary, not part of this unit. See
> [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
