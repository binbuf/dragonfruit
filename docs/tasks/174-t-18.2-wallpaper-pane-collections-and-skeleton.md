# T-18.2 — Wallpaper pane collections, skeleton, and attribution

| | |
|---|---|
| **Order** | 174 of 175 |
| **Slice** | T-18 |
| **Area** | apps/settings, design-system |
| **Depends on** | T-18.1b |
| **Blocks** | T-18.3 |
| **Hardware** | no |

> Parent design reference: [../18-wallpaper-content-provider.md](../design/tracks/18-wallpaper-content-provider.md) · ADR [0055](../design/adr/0055-online-wallpaper-content-provider.md)

## Goal

The Wallpaper pane shows Featured / Built-in / Custom sources, fills Featured
with fetched pictures, shimmers while they download, and shows attribution and
empty/offline/error states.

## Context (read first)

- `apps/settings/WallpaperPane.qml` — the current pane (hero card, two gradient
  collections, portal `Add Photo…`); restructure it into three rows.
- `apps/settings/SettingsBridge.*` — where the provider's `Items`/`Status`
  reach QML (mirror the existing `wallpaperPresets`/`wallpaperChooserAvailable`
  pattern).
- `design-system/components/` + `design-system/gallery/` + `design-system/tokens/`
  — new UI consumes components/tokens; gallery goldens enforce the visual floor.
- `docs/reference/System_Preferences.md` § Wallpaper — row order and the
  Apple-only rows to adapt or omit.
- ADR [0055](../design/adr/0055-online-wallpaper-content-provider.md) — states
  and attribution.

## Reference UI/UX (macOS screenshots)

Local art direction only (`docs/reference/macos/` never ships). Observed order:
current-wallpaper card + `Show on all Spaces`; `Dynamic Wallpapers`;
`Your Photos` (`Add Photo…`); `Landscape`; `Cityscape`. Ours is per-Space.
Adapt/omit Apple artwork names and the screensaver/clock buttons.

## Scope

- [ ] **Design-system skeleton**: add a `Skeleton` component — a grey slow
      animated gradient (shimmer) with tokens (`skeleton.base`,
      `skeleton.highlight`, duration/easing), a **reduced-motion static
      variant**, and a gallery golden.
- [ ] Restructure the pane into three groups:
      **Featured** (the six Dragonfruit categories from the provider),
      **Built-in** (the existing original gradients), **Custom**
      (`Add Photo…` portal; degrade to disabled when absent, as today).
- [ ] Featured tiles bind to the provider catalogue via `SettingsBridge`;
      while `Status=fetching` and no items exist, render `Skeleton` rectangles
      (accessible name: "Downloading wallpapers").
- [ ] Offline/empty/error states with translated copy: pictures "will be
      available soon"; a cold cache keeps the solid-color fallback.
- [ ] Attribution for the current fetched wallpaper: artist, license short
      name linked to `licenseUrl`, and the file page; sanitize the HTML
      `Artist` value (never render raw markup).
- [ ] Selecting a tile keeps the existing per-Space / `Show on all Spaces`
      behavior and writes `wallpaper.source`.
- [ ] Accessibility: accessible names include category + title + license;
      keyboard navigation and focus rings on tiles; reduced motion stops the
      shimmer.

## Explicitly deferred

- Favorites/collections and per-provider browsing (post-gate).
- Video/dynamic wallpapers (post-gate).

## Design notes

- Do not fetch from QML: the pane reads the provider's D-Bus catalogue only.
- Keep `wallpaperChooserAvailable` degradation for the Custom row.
- Skeleton must not animate under `accessibility.reduceMotion`.
- New strings are translatable (`qsTr`) for T-16.7.

## Acceptance (agent-completable)

- [ ] `tst_settings_wallpaper.qml` covers fetching/ready/offline/error,
      selection, and accessible names.
- [ ] Skeleton gallery golden committed; reduced-motion golden is static.
- [ ] Absence test: portal absent disables only the Custom row.

## Test plan

- `make qml-test` (settings wallpaper + absence) and the gallery golden test.
- Fixture the provider catalogue with `DF_*` (mirror `DF_SETTINGS_FIXTURE`).
- Nested capture of the shimmer and the filled pane.

## Hand-off

_(filled in by the implementing session)_

## Live visual check (required)

Launch `make demo`, open Wallpaper, and capture it while fetching and after it
fills. Inspect the image: three rows present, skeleton reads as "loading"
(not broken), attribution visible, no clipping or stray artifacts. Record the
capture path and verdict in the Hand-off.