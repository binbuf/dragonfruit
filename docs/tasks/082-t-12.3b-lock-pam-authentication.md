# T-12.3b — Lock PAM authentication

| | |
|---|---|
| **Order** | 82 of 168 |
| **Slice** | T-12 |
| **Area** | services/ (auth helper), shell/ (lock UI) |
| **Depends on** | T-12.3a |
| **Blocks** | T-12.3c |
| **Hardware** | no |

> Parent design reference: [../12-session-lock-idle.md](../design/tracks/12-session-lock-idle.md)

## Goal

Authenticate through PAM via a small helper; never a custom credential store.

## Scope

- Authentication via PAM through a small helper; never a custom credential store.

## Explicitly deferred

- Input capture and kill-resistance (T-12.3c).

## Acceptance (agent-completable)

- [ ] Correct credentials unlock; wrong ones do not; no credential storage.

## Test plan

- Headless: helper success/failure paths against a PAM test service.

## Hand-off

- T-12.3c hardens enforcement.

> Human sign-off (watch the demo, compare to design docs/goldens) is batched at
> the track boundary, not part of this unit. See
> [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
