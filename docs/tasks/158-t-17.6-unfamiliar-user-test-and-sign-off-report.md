# T-17.6 — Unfamiliar-user test and sign-off report

| | |
|---|---|
| **Order** | 158 of 168 |
| **Slice** | T-17 — The premium experience gate |
| **Area** | integration/ |
| **Depends on** | T-17.5b |
| **Blocks** | post-gate backlog |
| **Hardware** | no |

> Parent design reference: [../17-premium-gate.md](../design/tracks/17-premium-gate.md)

## Goal

Confirm the product reads as intended to someone who has not read the docs, and close the gate.

## Scope

- Unfamiliar user performs the loop without instructions; failures are legible.
- Side-by-side against design docs/reference; assets original.
- Post-gate backlog explicitly listed; sign-off report and reproduction commands committed.

## Explicitly deferred

- (none)

## Acceptance (agent-completable)

- [ ] Every checklist item ticked or explicitly waived with a reason; report and captures committed; `make check` and `make e2e` green on the release commit.

## Test plan

- Human unfamiliar-user test; document comparison.

## Hand-off

- Post-gate backlog (desktop icons, Spotlight search, GOA, NVIDIA/HDR, printer breadth).

## Live visual check (required)

Before reporting done, run the app and **look at it**:

- Launch the nested session (`make demo`), open the surface this task changed,
  and capture it (e.g. `spectacle -b -n -f -o /tmp/<task>.png`).
- Inspect the image: all UI/UX must be present and read as intended in the
  compositor window — layout, materials, text, and no stray artifacts.
- A green `make e2e` is necessary but **not sufficient**: the headless suites
  render no pixels, so they miss exactly this class of bug. If the task has no
  surface of its own, still launch the demo and confirm the desktop renders.
- Note the capture path and your verdict in the Hand-off.

> The per-task live visual check above is part of this unit. Only the *human*
> sign-off (watch the demo, compare to design docs/goldens) is batched at the
> track boundary. See [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
