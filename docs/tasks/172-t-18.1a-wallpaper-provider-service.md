# T-18.1a — Wallpaper provider service

| | |
|---|---|
| **Order** | 172 of 175 |
| **Slice** | T-18 |
| **Area** | services/wallpaperd/ |
| **Depends on** | T-09.3 |
| **Blocks** | T-18.1b |
| **Hardware** | no |

> Parent design reference: [../18-wallpaper-content-provider.md](../design/tracks/18-wallpaper-content-provider.md) · ADR [0055](../design/adr/0055-online-wallpaper-content-provider.md)

## Goal

A restartable session service that fetches Wikimedia Commons Featured Pictures
into a local cache with attribution metadata, selects the deterministic Nature
default, refreshes weekly, and degrades cleanly offline.

## Context (read first)

- ADR [0055](../design/adr/0055-online-wallpaper-content-provider.md) — the
  category map, query shape, default rule, and attribution contract.
- `services/power/` — the restartable adapter/service shape to follow
  (`adapter.rs` / `source.rs` / `model.rs`, fixture tests, D-Bus surface).
- `services/settingsd/src/schema.rs` — key ownership model the provider will
  extend in T-18.1b.
- `compositor/src/wallpaper.rs` — the consumer; it only needs a local path, so
  nothing here touches Wayland.
- `docs/design/07-system-integration.md` — absence-is-normal discipline.

## Scope

- [ ] New crate `services/wallpaperd` (Rust, MIT, workspace member + service
      registration like the other `services/`).
- [ ] Add one permissively-licensed HTTP client (prefer `ureq`; MIT/Apache-2.0).
      No GPL/AGPL dependency (`docs/licensing.md`).
- [ ] Category map constant for the six categories in ADR 0055.
- [ ] Query builder matching the validated shape: `generator=categorymembers`,
      `gcmnamespace=6`, `gcmlimit=10`, `gcmsort=timestamp`, `gcmdir=desc`,
      `prop=imageinfo`, `iiprop=url|size|mime|extmetadata`, `iiurlwidth=3840`,
      `format=json`, `formatversion=2`; follow `continue`.
- [ ] Post-filter `ns==6` and `mime` starting `image/`; dedupe by `pageid`;
      cap at 10 per category.
- [ ] Normalize URLs: strip the `utm_*` query Wikimedia appends.
- [ ] Download thumbnails to
      `$XDG_CACHE_HOME/dragonfruit/wallpapers/<category>/<pageid>.<ext>` and
      write an `index.json` with `title`, `artist` (HTML stripped), `licenseShortName`,
      `licenseUrl`, `pageUrl`, `description`, `category`, `pageid`, `width`,
      `height`, `fetchedAt`.
- [ ] Deterministic default: first Nature entry under timestamp-desc.
- [ ] First-run fetch when the cache is empty; weekly re-check from a persisted
      `lastFetch`; one in-flight refresh at a time.
- [ ] D-Bus interface `org.dragonfruit.Wallpaper1`:
      properties `Status` (`idle|fetching|ready|offline`), `LastFetch`,
      `DefaultSource`, `Items`; method `Refresh`; signals `ItemsChanged`,
      `StatusChanged`.
- [ ] Offline/error path: keep the cache, set `offline`, no retry storm, no
      crash; never fetch on the compositor/shell frame path.

## Explicitly deferred

- Settings keys and default-source wiring → T-18.1b.
- Pane UI, skeleton, attribution display → T-18.2.
- Licensing policy text and capture → T-18.3.

## Design notes

- Send a descriptive `User-Agent`; cap concurrent downloads and per-request
  timeouts.
- Model the provider as an interface so a second content source can be added
  later without changing the D-Bus surface.
- The service is restartable; a cold start rebuilds the catalogue from the
  cache without a network round-trip.

## Acceptance (agent-completable)

- [ ] Headless tests cover parser, filtering, dedup, URL normalization, cache
      layout, default selection, and the offline path.
- [ ] The service runs, serves `Items`/`DefaultSource`, and survives the
      network being absent.

## Test plan

- Fixture the Commons JSON (the validated `Category:Featured pictures of
  landscapes` response is a good base) and unit-test the query builder and the
  parser.
- A local mock server (or a `file://`-style source seam) drives the download
  and cache tests without the network.
- `make e2e` stays green.

## Hand-off

_(filled in by the implementing session)_

## Live visual check (required)

This unit has no surface of its own; still launch `make demo` and confirm the
desktop renders and the service starts without error, then note that in the
Hand-off.