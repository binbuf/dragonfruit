# T-12.4a — Idle timers

| | |
|---|---|
| **Order** | 84 of 171 |
| **Slice** | T-12 |
| **Area** | services/session/ |
| **Depends on** | T-12.3c |
| **Blocks** | T-12.4b |
| **Hardware** | no |

> Parent design reference: [../12-session-lock-idle.md](../design/tracks/12-session-lock-idle.md)

## Goal

Idle timers (dim/blank/lock/suspend) driven from policy keys.

## Scope

- Idle timers (dim/blank/lock/suspend) from policy keys.

## Explicitly deferred

- Inhibitors and wake restore (T-12.4b).

## Acceptance (agent-completable)

- [ ] Each idle stage fires at its configured delay.

## Test plan

- Headless: fake clock drives each stage.

## Hand-off

- T-12.4b adds inhibitors.

## Live visual check (required)

Before reporting done, run the app and **look at it**:

- Launch the nested session (`make demo`), open the surface this task changed,
  and capture it (e.g. `spectacle -b -n -f -o /tmp/<task>.png`).
- Inspect the image: all UI/UX must be present and read as intended in the
  compositor window — layout, materials, text, and no stray artifacts.
- A green `make e2e` is necessary but **not sufficient**: the headless suites
  render no pixels, so they miss exactly this class of bug. If the task has no
  surface of its own, still launch the demo and confirm the desktop renders.
- Note the capture path and your verdict in the Hand-off.

> The per-task live visual check above is part of this unit. Only the *human*
> sign-off (watch the demo, compare to design docs/goldens) is batched at the
> track boundary. See [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
