# T-09.4 — Desktop & Dock pane

| | |
|---|---|
| **Order** | 52 of 168 |
| **Slice** | T-09 — Settings Wave 1 |
| **Area** | apps/settings/ |
| **Depends on** | T-09.3 |
| **Blocks** | T-09.5 |
| **Hardware** | no |

> Parent design reference: [../09-settings-app-wave-1.md](../design/tracks/09-settings-app-wave-1.md)

## Context (read first)

- [`docs/reference/System_Preferences.md`](../reference/System_Preferences.md)
  — the near-verbatim System Settings information architecture, row labels,
  and control types this task mirrors (local macOS captures live in
  `docs/reference/macos/` and never ship).

## Goal

Auto-hide, magnification, size, position, minimize-into-icon, indicators, and recents.

## Scope

- All `dock.*` keys from T-08, applied live; the Dock re-lays-out without restart.

## Explicitly deferred

- External-drop/options behaviors owned by the Dock.

## Reference UI/UX (macOS screenshots)

Local art direction only: images in `docs/reference/macos/` (never shipped;
see [14-risks.md](../design/14-risks.md)), distilled in
[System_Preferences.md](../reference/System_Preferences.md); target the
Desktop & Dock pane near-verbatim.

**Sections and rows (observed order):**

- **Dock**:
  - `Size` — slider, `Small`/`Large` captions → `dock.size`
  - `Magnification` — slider, `Off`/`Small`/`Large` → `dock.magnification`
  - `Dock position on screen` — popup (`Bottom`) → `dock.position`
  - `Minimized window animation` — popup (`Genie Effect`) →
    `dock.minimizedAnimation` (genie/scale/none)
  - `Window title bar double-click action` — popup (`Zoom`) →
    `dock.titlebarDoubleClick` (zoom/minimize/none)
  - `Minimize windows into application icon` — toggle →
    `dock.minimizeIntoTileIcon`
  - `Automatically hide and show the Dock` — toggle → `dock.autohide`
  - `Animate opening applications` — toggle → `dock.animateOpening`
  - `Show indicators for open applications` — toggle → `dock.showIndicators`
  - `Show suggested and recent apps in Dock` — toggle → `dock.showRecentApps`
- **Desktop & Stage Manager**:
  - `Show items` — segmented (`On Desktop`/`In Stage Manager`) → Desktop
    Reveal (T-05.5) layer; `In Stage Manager` omitted
  - `Click wallpaper to show desktop` — popup (`Always`) → Desktop Reveal
    (T-05.5)
  - `Stage Manager` — toggle; omitted (Apple-only)
  - `Show recent apps in Stage Manager` — toggle; omitted (Apple-only)

Grouped inset sections, right-aligned controls, popups with a trailing
chevron; `dock.pinned` has no reference row (macOS reorders by drag).

**Adapt or omit (Apple-only):**

- Stage Manager and `Show recent apps in Stage Manager`.
- `Show suggested and recent apps in Dock` if no backend (decide at
  implementation).
- Apple-account touchpoints (sidebar/header).

## Acceptance (agent-completable)

- [ ] Every Dock control applies live within one interaction beat.

## Test plan

- QML test; nested capture against the Dock.

## Hand-off

- T-09.5 continues the wave.

## Live visual check (required)

Before reporting done, run the app and **look at it**:

- Launch the nested session (`make demo`), open the surface this task changed,
  and capture it (e.g. `spectacle -b -n -f -o /tmp/<task>.png`).
- Inspect the image: all UI/UX must be present and read as intended in the
  compositor window — layout, materials, text, and no stray artifacts.
- A green `make e2e` is necessary but **not sufficient**: the headless suites
  render no pixels, so they miss exactly this class of bug. If the task has no
  surface of its own, still launch the demo and confirm the desktop renders.
- Note the capture path and your verdict in the Hand-off.

> The per-task live visual check above is part of this unit. Only the *human*
> sign-off (watch the demo, compare to design docs/goldens) is batched at the
> track boundary. See [../SLICING-REVIEW.md](../SLICING-REVIEW.md).
