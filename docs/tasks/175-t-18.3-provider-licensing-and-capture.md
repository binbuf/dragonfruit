# T-18.3 — Provider licensing, absence matrix, and capture

| | |
|---|---|
| **Order** | 175 of 175 |
| **Slice** | T-18 |
| **Area** | docs/, tests, acceptance |
| **Depends on** | T-18.2 |
| **Blocks** | T-17 |
| **Hardware** | no |

> Parent design reference: [../18-wallpaper-content-provider.md](../design/tracks/18-wallpaper-content-provider.md) · ADR [0055](../design/adr/0055-online-wallpaper-content-provider.md)

## Goal

Land the fetched-content licensing policy, prove the first-run/offline/weekly
matrix, hand i18n/a11y coverage to T-16, and capture the track's demo.

## Context (read first)

- `docs/licensing.md` — MIT repo-wide; the "no copyleft dependency" and
  "original assets" rules; distinct from third-party fetched content.
- `docs/design/14-risks.md` — Apple-artwork rule and the public-release gate.
- ADR [0055](../design/adr/0055-online-wallpaper-content-provider.md).
- `docs/SLICING-REVIEW.md` — track-boundary sign-off.

## Scope

- [ ] Add a "Fetched third-party content (wallpaper)" section to
      `docs/licensing.md`: fetched at runtime, never bundled; attribution shown;
      license diversity (public domain, CC BY, CC BY-SA, FAL); share-alike/FAL
      content is not represented as original; the HTTP client dependency stays
      permissive.
- [ ] Absence/state matrix: cold cache + no network, warm cache + no network,
      provider restart, settingsd restart, portal absent, API error/rate limit.
- [ ] i18n/a11y hand-off notes for T-16.7 and T-16.6 so the new copy, skeleton,
      and attribution rows are swept.
- [ ] Commit the capture (`docs/captures/t18-wallpaper.*`) and note it in
      `PROGRESS.md`.
- [ ] Tick this track's acceptance boxes in
      [18-wallpaper-content-provider.md](../design/tracks/18-wallpaper-content-provider.md).

## Explicitly deferred

- Human visual-floor sign-off → T-17 (batched at the track boundary).

## Design notes

- Licensing is a first-public-release blocker; do not ship a package that
  bundles any fetched image or omits attribution.
- Record the manual parts (real network, real gallery) honestly rather than
  claiming them in the headless path.

## Acceptance (agent-completable)

- [ ] The licensing section is reviewed against ADR 0055 and the risk doc.
- [ ] The absence/state matrix is committed with reproductions.
- [ ] Capture committed; `make e2e`, `make check`, and `make soak` unchanged.
- [ ] Track acceptance boxes ticked.

## Test plan

- Run the matrix steps and record outputs.
- Confirm no fetched bytes are present in any package artifact (packaging is
  the hardware rail, but assert the install manifest here if available).

## Hand-off

_(filled in by the implementing session)_

## Live visual check (required)

Launch `make demo`, delete the cache, and watch the first-run fill through to
the default Nature wallpaper; capture it. Then break the network and confirm
the cached desktop still renders. Record paths and verdict in the Hand-off.